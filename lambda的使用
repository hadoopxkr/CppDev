语法：
    [捕获参数列表](参数列表)-> muteable 返回值{}
    //默认捕获变量当前的值
    [=] 捕获所有的参数  以值的形式
    [&] 捕获所有的参数  以引用的形式
    
    muteable 表示可以改变捕获的变量的值 如果是值引用的模式 只在函数体有效 并不会改变原始的参数值
    
    eg:
    int a = 10;
   
    auto f = [a](int n) -> int {
    
      return a + n;
    }
    
    f(5);
    
    
